{% extends '::base.html.twig' %}

{% block body %}
    <div class="wrap">
    <nav class="navbar navbar-inverse" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                        aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                <a  href="#">  <img src="{{ asset('bundles/webtaximain/images/LOGO.png') }}" /> </a>

            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    {% if app.request.attributes.get('_route') == 'webtaxi_main_map_index' %}
                    <li class="active">
                {% else %}
                    <li>
                        {% endif %}
                        <a href="{{ path('webtaxi_main_map_index') }}">Rask vežėją</a>
                    </li>

                    {% if app.request.attributes.get('_route') == 'webtaxi_main_findtraveller_index' %}
                    <li class="active">
                {% else %}
                    <li>
                        {% endif %}
                        <a href="{{ path('webtaxi_main_findtraveller_index') }}">Rask pakeleivį</a>
                    </li>

                </ul>


                <span class="navbar-text navbar-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {% if app.request.attributes.get('_route') == 'webtaxi_main_usersettings_index' %}
                            <span class="active">
                        {% else %}
                            <span>
                        {% endif %}
                        <a href="{{ path('fos_user_profile_edit') }}">{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                Prisijungęs kaip {{ app.security.getToken().getUser() }}
                            {% endif %}</a>

                        <a href="{{ path('fos_user_security_logout') }}">{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                &nbsp; <img src="{{ asset('bundles/webtaximain/images/ic_logout.png') }}" />
                            {% endif %}</a>




                        </span>
                    {% else %}
                        {% if app.request.attributes.get('_route') == 'fos_user_security_login' %}
                            <span class="active">
                        {% else %}
                            <span>
                        {% endif %}
                        <a href="{{ path('fos_user_security_login') }}">Jungtis</a>
                        </span>
                    {% endif %}


                </span>
            </div>

        </div>


    </nav>




    <div class="container" id="container">
        {% block content %}{% endblock %}
    </div>
   </div>



    <div class="navbar navbar-fixed-bottom navbar-default">
        <p class="navbar-text">WebTaxi Team 2014</p>
    </div>
{% endblock %}
